"""Inline closed-form J_pq^static = ∫∫ (s-α)^p (s'-A)^q / √((s-s')²+a²)
on a single straight edge, for p, q ∈ {0..2}.

Generated by scripts/derive_bspline_static_moments.py (do not edit).
"""

import numpy as np


def J_static_moment(p, q, alpha, beta, A, B, a):
    """One static moment integral (without the 1/(4π) prefactor).
    alpha/beta/A/B/a broadcast as numpy arrays.
    """
    if (p, q) == (0, 0):
        return (
            -A * np.arcsinh(A / a - alpha / a)
            + A * np.arcsinh(A / a - beta / a)
            + B * np.arcsinh(B / a - alpha / a)
            - B * np.arcsinh(B / a - beta / a)
            + alpha * np.arcsinh(A / a - alpha / a)
            - alpha * np.arcsinh(B / a - alpha / a)
            - beta * np.arcsinh(A / a - beta / a)
            + beta * np.arcsinh(B / a - beta / a)
            + np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
        )
    elif (p, q) == (0, 1):
        return (
            (1 / 2) * A**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 2 * A**2 * np.arcsinh(A / a - beta / a)
            - A * B * np.arcsinh(B / a - alpha / a)
            + A * B * np.arcsinh(B / a - beta / a)
            - A * alpha * np.arcsinh(A / a - alpha / a)
            + A * alpha * np.arcsinh(B / a - alpha / a)
            + A * beta * np.arcsinh(A / a - beta / a)
            - A * beta * np.arcsinh(B / a - beta / a)
            - 1 / 4 * A * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (1 / 4) * A * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + A * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - A * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * B**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * B**2 * np.arcsinh(B / a - beta / a)
            - 3 / 4 * B * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (3 / 4) * B * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * a**2 * np.arcsinh((-A + alpha) / a)
            - 1 / 2 * a**2 * np.arcsinh((-A + beta) / a)
            - 1 / 2 * a**2 * np.arcsinh((-B + alpha) / a)
            + (1 / 2) * a**2 * np.arcsinh((-B + beta) / a)
            + (1 / 4) * a**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 4 * a**2 * np.arcsinh(A / a - beta / a)
            - 1 / 4 * a**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 4) * a**2 * np.arcsinh(B / a - beta / a)
            + (1 / 2) * alpha**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 2 * alpha**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 4) * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 1 / 4 * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 2 * beta**2 * np.arcsinh(A / a - beta / a)
            + (1 / 2) * beta**2 * np.arcsinh(B / a - beta / a)
            - 1 / 4 * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 4) * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (-1 / 2 * A + (1 / 2) * alpha)
            * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - (-1 / 2 * A + (1 / 2) * beta)
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - (-1 / 2 * B + (1 / 2) * alpha)
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (-1 / 2 * B + (1 / 2) * beta)
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
        )
    elif (p, q) == (0, 2):
        return (
            -1 / 3 * A**3 * np.arcsinh(A / a - alpha / a)
            + (1 / 3) * A**3 * np.arcsinh(A / a - beta / a)
            + A**2 * B * np.arcsinh(B / a - alpha / a)
            - A**2 * B * np.arcsinh(B / a - beta / a)
            + A**2 * alpha * np.arcsinh(A / a - alpha / a)
            - A**2 * alpha * np.arcsinh(B / a - alpha / a)
            - A**2 * beta * np.arcsinh(A / a - beta / a)
            + A**2 * beta * np.arcsinh(B / a - beta / a)
            + (11 / 18) * A**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 11 / 18 * A**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - A**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + A**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - A * B**2 * np.arcsinh(B / a - alpha / a)
            + A * B**2 * np.arcsinh(B / a - beta / a)
            + (3 / 2) * A * B * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 3 / 2 * A * B * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * A * a**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 2 * A * a**2 * np.arcsinh(A / a - beta / a)
            + (1 / 2) * A * a**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * A * a**2 * np.arcsinh(B / a - beta / a)
            - A * alpha**2 * np.arcsinh(A / a - alpha / a)
            + A * alpha**2 * np.arcsinh(B / a - alpha / a)
            - 11 / 9 * A * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (1 / 2) * A * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + A * beta**2 * np.arcsinh(A / a - beta / a)
            - A * beta**2 * np.arcsinh(B / a - beta / a)
            + (11 / 9) * A * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 2 * A * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 3) * B**3 * np.arcsinh(B / a - alpha / a)
            - 1 / 3 * B**3 * np.arcsinh(B / a - beta / a)
            - 11 / 18 * B**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (11 / 18) * B**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - B * a**2 * np.arcsinh(B / a - alpha / a)
            + B * a**2 * np.arcsinh(B / a - beta / a)
            - 5 / 18 * B * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (5 / 18) * B * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * a**2 * alpha * np.arcsinh(A / a - alpha / a)
            + (1 / 2) * a**2 * alpha * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * a**2 * beta * np.arcsinh(A / a - beta / a)
            - 1 / 2 * a**2 * beta * np.arcsinh(B / a - beta / a)
            - 2 / 9 * a**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (2 / 9) * a**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (13 / 18) * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 13 / 18 * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 3) * alpha**3 * np.arcsinh(A / a - alpha / a)
            - 1 / 3 * alpha**3 * np.arcsinh(B / a - alpha / a)
            + (11 / 18) * alpha**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 1 / 9 * alpha**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 3 * beta**3 * np.arcsinh(A / a - beta / a)
            + (1 / 3) * beta**3 * np.arcsinh(B / a - beta / a)
            - 11 / 18 * beta**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 9) * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            * (A * B - A * alpha - 1 / 2 * B**2 + (1 / 2) * a**2 + (1 / 2) * alpha**2)
            + np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            * (A * B - A * beta - 1 / 2 * B**2 + (1 / 2) * a**2 + (1 / 2) * beta**2)
            - (
                -2 * A * B**2
                - 2 * A * a**2
                + A * (B**2 + a**2)
                + (1 / 2) * B**3
                + (1 / 2) * B * a**2
                + B * (A * B - 1 / 2 * B**2 + (1 / 2) * a**2)
            )
            * np.arcsinh((-B + alpha) / a)
            + (
                -2 * A * B**2
                - 2 * A * a**2
                + A * (B**2 + a**2)
                + (1 / 2) * B**3
                + (1 / 2) * B * a**2
                + B * (A * B - 1 / 2 * B**2 + (1 / 2) * a**2)
            )
            * np.arcsinh((-B + beta) / a)
        )
    elif (p, q) == (1, 0):
        return (
            -1 / 2 * A**2 * np.arcsinh(A / a - alpha / a)
            + (1 / 2) * A**2 * np.arcsinh(A / a - beta / a)
            + A * alpha * np.arcsinh(A / a - alpha / a)
            - A * alpha * np.arcsinh(A / a - beta / a)
            + (3 / 4) * A * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 3 / 4 * A * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 2) * B**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * B**2 * np.arcsinh(B / a - beta / a)
            - B * alpha * np.arcsinh(B / a - alpha / a)
            + B * alpha * np.arcsinh(B / a - beta / a)
            - 3 / 4 * B * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (3 / 4) * B * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 4) * a**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 4 * a**2 * np.arcsinh(A / a - beta / a)
            - 1 / 4 * a**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 4) * a**2 * np.arcsinh(B / a - beta / a)
            - 1 / 2 * alpha**2 * np.arcsinh(A / a - alpha / a)
            + (1 / 2) * alpha**2 * np.arcsinh(B / a - alpha / a)
            + alpha * beta * np.arcsinh(A / a - beta / a)
            - alpha * beta * np.arcsinh(B / a - beta / a)
            - 3 / 4 * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + alpha * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (3 / 4) * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * beta**2 * np.arcsinh(A / a - beta / a)
            + (1 / 2) * beta**2 * np.arcsinh(B / a - beta / a)
            - 1 / 4 * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 4) * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
        )
    elif (p, q) == (1, 1):
        return (
            (1 / 6) * A**3 * np.arcsinh(A / a - alpha / a)
            - 1 / 6 * A**3 * np.arcsinh(A / a - beta / a)
            - 1 / 2 * A**2 * alpha * np.arcsinh(A / a - alpha / a)
            + (1 / 2) * A**2 * alpha * np.arcsinh(A / a - beta / a)
            - 11 / 36 * A**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (11 / 36) * A**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 2 * A * B**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * A * B**2 * np.arcsinh(B / a - beta / a)
            + A * B * alpha * np.arcsinh(B / a - alpha / a)
            - A * B * alpha * np.arcsinh(B / a - beta / a)
            + (3 / 4) * A * B * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 3 / 4 * A * B * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 4 * A * a**2 * np.arcsinh(A / a - alpha / a)
            + (1 / 4) * A * a**2 * np.arcsinh(A / a - beta / a)
            + (1 / 4) * A * a**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 4 * A * a**2 * np.arcsinh(B / a - beta / a)
            + (1 / 2) * A * alpha**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 2 * A * alpha**2 * np.arcsinh(B / a - alpha / a)
            - A * alpha * beta * np.arcsinh(A / a - beta / a)
            + A * alpha * beta * np.arcsinh(B / a - beta / a)
            + (11 / 18) * A * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 3 / 4 * A * alpha * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 3 / 4 * A * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + A * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * A * beta**2 * np.arcsinh(A / a - beta / a)
            - 1 / 2 * A * beta**2 * np.arcsinh(B / a - beta / a)
            + (5 / 36) * A * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 4 * A * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 3) * B**3 * np.arcsinh(B / a - alpha / a)
            - 1 / 3 * B**3 * np.arcsinh(B / a - beta / a)
            - 1 / 2 * B**2 * alpha * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * B**2 * alpha * np.arcsinh(B / a - beta / a)
            - 4 / 9 * B**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (4 / 9) * B**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (5 / 36) * B * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 4 * B * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 9) * B * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 4) * a**2 * alpha * np.arcsinh(A / a - alpha / a)
            - 1 / 4 * a**2 * alpha * np.arcsinh(A / a - beta / a)
            - 1 / 4 * a**2 * alpha * np.arcsinh(B / a - alpha / a)
            + (1 / 4) * a**2 * alpha * np.arcsinh(B / a - beta / a)
            + (1 / 9) * a**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 1 / 9 * a**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 9 * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (1 / 9) * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 6 * alpha**3 * np.arcsinh(A / a - alpha / a)
            + (1 / 6) * alpha**3 * np.arcsinh(B / a - alpha / a)
            - 11 / 36 * alpha**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (11 / 36) * alpha**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (1 / 2) * alpha * beta**2 * np.arcsinh(A / a - beta / a)
            - 1 / 2 * alpha * beta**2 * np.arcsinh(B / a - beta / a)
            + (3 / 4) * alpha * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 3 / 4 * alpha * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 3 * beta**3 * np.arcsinh(A / a - beta / a)
            + (1 / 3) * beta**3 * np.arcsinh(B / a - beta / a)
            - 4 / 9 * beta**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (4 / 9) * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
        )
    elif (p, q) == (1, 2):
        return (
            -1 / 12 * A**4 * np.arcsinh(A / a - alpha / a)
            + (1 / 12) * A**4 * np.arcsinh(A / a - beta / a)
            + (1 / 3) * A**3 * alpha * np.arcsinh(A / a - alpha / a)
            - 1 / 3 * A**3 * alpha * np.arcsinh(A / a - beta / a)
            + (25 / 144) * A**3 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 25 / 144 * A**3 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 2) * A**2 * B**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * A**2 * B**2 * np.arcsinh(B / a - beta / a)
            - A**2 * B * alpha * np.arcsinh(B / a - alpha / a)
            + A**2 * B * alpha * np.arcsinh(B / a - beta / a)
            - 3 / 4 * A**2 * B * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (3 / 4) * A**2 * B * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 4) * A**2 * a**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 4 * A**2 * a**2 * np.arcsinh(A / a - beta / a)
            - 1 / 4 * A**2 * a**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 4) * A**2 * a**2 * np.arcsinh(B / a - beta / a)
            - 1 / 2 * A**2 * alpha**2 * np.arcsinh(A / a - alpha / a)
            + (1 / 2) * A**2 * alpha**2 * np.arcsinh(B / a - alpha / a)
            + A**2 * alpha * beta * np.arcsinh(A / a - beta / a)
            - A**2 * alpha * beta * np.arcsinh(B / a - beta / a)
            - 25 / 48 * A**2 * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (11 / 18) * A**2 * alpha * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (3 / 4) * A**2 * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - A**2 * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * A**2 * beta**2 * np.arcsinh(A / a - beta / a)
            + (1 / 2) * A**2 * beta**2 * np.arcsinh(B / a - beta / a)
            - 13 / 144 * A**2 * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 4) * A**2 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 2 / 3 * A * B**3 * np.arcsinh(B / a - alpha / a)
            + (2 / 3) * A * B**3 * np.arcsinh(B / a - beta / a)
            + A * B**2 * alpha * np.arcsinh(B / a - alpha / a)
            - A * B**2 * alpha * np.arcsinh(B / a - beta / a)
            + (8 / 9) * A * B**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 8 / 9 * A * B**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 5 / 18 * A * B * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (1 / 2) * A * B * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 2 / 9 * A * B * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * A * a**2 * alpha * np.arcsinh(A / a - alpha / a)
            + (1 / 2) * A * a**2 * alpha * np.arcsinh(A / a - beta / a)
            + (1 / 2) * A * a**2 * alpha * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * A * a**2 * alpha * np.arcsinh(B / a - beta / a)
            - 55 / 288 * A * a**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (55 / 288) * A * a**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (2 / 9) * A * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 2 / 9 * A * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 3) * A * alpha**3 * np.arcsinh(A / a - alpha / a)
            - 1 / 3 * A * alpha**3 * np.arcsinh(B / a - alpha / a)
            + (25 / 48) * A * alpha**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 11 / 18 * A * alpha**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - A * alpha * beta**2 * np.arcsinh(A / a - beta / a)
            + A * alpha * beta**2 * np.arcsinh(B / a - beta / a)
            - 11 / 9 * A * alpha * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (3 / 2) * A * alpha * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (2 / 3) * A * beta**3 * np.arcsinh(A / a - beta / a)
            - 2 / 3 * A * beta**3 * np.arcsinh(B / a - beta / a)
            + (101 / 144) * A * beta**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 8 / 9 * A * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 4) * B**4 * np.arcsinh(B / a - alpha / a)
            - 1 / 4 * B**4 * np.arcsinh(B / a - beta / a)
            - 1 / 3 * B**3 * alpha * np.arcsinh(B / a - alpha / a)
            + (1 / 3) * B**3 * alpha * np.arcsinh(B / a - beta / a)
            - 5 / 16 * B**3 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (5 / 16) * B**3 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (7 / 144) * B**2 * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 9 * B**2 * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 16) * B**2 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 32 * B * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (1 / 32) * B * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (13 / 144)
            * B
            * alpha**2
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 5 / 18 * B * alpha * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (3 / 16) * B * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 32 * a**4 * np.arcsinh(A / a - alpha / a)
            + (1 / 32) * a**4 * np.arcsinh(A / a - beta / a)
            + (1 / 32) * a**4 * np.arcsinh(B / a - alpha / a)
            - 1 / 32 * a**4 * np.arcsinh(B / a - beta / a)
            + (1 / 4) * a**2 * alpha**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 4 * a**2 * alpha**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * a**2 * alpha * beta * np.arcsinh(A / a - beta / a)
            + (1 / 2) * a**2 * alpha * beta * np.arcsinh(B / a - beta / a)
            + (55 / 288)
            * a**2
            * alpha
            * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 2 / 9 * a**2 * alpha * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 55 / 288 * a**2 * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (2 / 9) * a**2 * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 4) * a**2 * beta**2 * np.arcsinh(A / a - beta / a)
            - 1 / 4 * a**2 * beta**2 * np.arcsinh(B / a - beta / a)
            + (1 / 32) * a**2 * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 32 * a**2 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 12 * alpha**4 * np.arcsinh(A / a - alpha / a)
            + (1 / 12) * alpha**4 * np.arcsinh(B / a - alpha / a)
            - 25 / 144 * alpha**3 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (25 / 144) * alpha**3 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (1 / 3) * alpha * beta**3 * np.arcsinh(A / a - beta / a)
            - 1 / 3 * alpha * beta**3 * np.arcsinh(B / a - beta / a)
            + (11 / 18)
            * alpha
            * beta**2
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 11 / 18 * alpha * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 4 * beta**4 * np.arcsinh(A / a - beta / a)
            + (1 / 4) * beta**4 * np.arcsinh(B / a - beta / a)
            - 7 / 16 * beta**3 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (7 / 16) * beta**3 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
        )
    elif (p, q) == (2, 0):
        return (
            -1 / 3 * A**3 * np.arcsinh(A / a - alpha / a)
            + (1 / 3) * A**3 * np.arcsinh(A / a - beta / a)
            + A**2 * alpha * np.arcsinh(A / a - alpha / a)
            - A**2 * alpha * np.arcsinh(A / a - beta / a)
            + (11 / 18) * A**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 11 / 18 * A**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 2) * A * a**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 2 * A * a**2 * np.arcsinh(A / a - beta / a)
            - A * alpha**2 * np.arcsinh(A / a - alpha / a)
            + A * alpha**2 * np.arcsinh(A / a - beta / a)
            - 11 / 9 * A * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (3 / 2) * A * alpha * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 5 / 18 * A * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 3) * B**3 * np.arcsinh(B / a - alpha / a)
            - 1 / 3 * B**3 * np.arcsinh(B / a - beta / a)
            - B**2 * alpha * np.arcsinh(B / a - alpha / a)
            + B**2 * alpha * np.arcsinh(B / a - beta / a)
            - 11 / 18 * B**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (11 / 18) * B**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * B * a**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * B * a**2 * np.arcsinh(B / a - beta / a)
            + B * alpha**2 * np.arcsinh(B / a - alpha / a)
            - B * alpha**2 * np.arcsinh(B / a - beta / a)
            + (11 / 9) * B * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 3 / 2 * B * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (5 / 18) * B * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * a**2 * alpha * np.arcsinh(A / a - alpha / a)
            + (1 / 2) * a**2 * alpha * np.arcsinh(A / a - beta / a)
            + (1 / 2) * a**2 * alpha * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * a**2 * alpha * np.arcsinh(B / a - beta / a)
            - 2 / 9 * a**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (2 / 9) * a**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (2 / 9) * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 2 / 9 * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 3) * alpha**3 * np.arcsinh(A / a - alpha / a)
            - 1 / 3 * alpha**3 * np.arcsinh(B / a - alpha / a)
            - alpha**2 * beta * np.arcsinh(A / a - beta / a)
            + alpha**2 * beta * np.arcsinh(B / a - beta / a)
            + (11 / 18) * alpha**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - alpha**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 11 / 18 * alpha**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + alpha**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + alpha * beta**2 * np.arcsinh(A / a - beta / a)
            - alpha * beta**2 * np.arcsinh(B / a - beta / a)
            + (1 / 2) * alpha * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 2 * alpha * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 3 * beta**3 * np.arcsinh(A / a - beta / a)
            + (1 / 3) * beta**3 * np.arcsinh(B / a - beta / a)
            - 1 / 9 * beta**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 9) * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
        )
    elif (p, q) == (2, 1):
        return (
            (1 / 12) * A**4 * np.arcsinh(A / a - alpha / a)
            - 1 / 12 * A**4 * np.arcsinh(A / a - beta / a)
            - 1 / 3 * A**3 * alpha * np.arcsinh(A / a - alpha / a)
            + (1 / 3) * A**3 * alpha * np.arcsinh(A / a - beta / a)
            - 25 / 144 * A**3 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (25 / 144) * A**3 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 4 * A**2 * a**2 * np.arcsinh(A / a - alpha / a)
            + (1 / 4) * A**2 * a**2 * np.arcsinh(A / a - beta / a)
            + (1 / 2) * A**2 * alpha**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 2 * A**2 * alpha**2 * np.arcsinh(A / a - beta / a)
            + (25 / 48) * A**2 * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 11 / 18 * A**2 * alpha * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (13 / 144) * A**2 * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 3 * A * B**3 * np.arcsinh(B / a - alpha / a)
            + (1 / 3) * A * B**3 * np.arcsinh(B / a - beta / a)
            + A * B**2 * alpha * np.arcsinh(B / a - alpha / a)
            - A * B**2 * alpha * np.arcsinh(B / a - beta / a)
            + (11 / 18) * A * B**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 11 / 18 * A * B**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * A * B * a**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * A * B * a**2 * np.arcsinh(B / a - beta / a)
            - A * B * alpha**2 * np.arcsinh(B / a - alpha / a)
            + A * B * alpha**2 * np.arcsinh(B / a - beta / a)
            - 11 / 9 * A * B * alpha * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (3 / 2) * A * B * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 5 / 18 * A * B * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * A * a**2 * alpha * np.arcsinh(A / a - alpha / a)
            - 1 / 2 * A * a**2 * alpha * np.arcsinh(A / a - beta / a)
            - 1 / 2 * A * a**2 * alpha * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * A * a**2 * alpha * np.arcsinh(B / a - beta / a)
            + (55 / 288) * A * a**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 55 / 288 * A * a**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 2 / 9 * A * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (2 / 9) * A * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 3 * A * alpha**3 * np.arcsinh(A / a - alpha / a)
            + (1 / 3) * A * alpha**3 * np.arcsinh(B / a - alpha / a)
            + A * alpha**2 * beta * np.arcsinh(A / a - beta / a)
            - A * alpha**2 * beta * np.arcsinh(B / a - beta / a)
            - 25 / 48 * A * alpha**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (3 / 4) * A * alpha**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (11 / 18) * A * alpha**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - A * alpha**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - A * alpha * beta**2 * np.arcsinh(A / a - beta / a)
            + A * alpha * beta**2 * np.arcsinh(B / a - beta / a)
            - 5 / 18 * A * alpha * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 2) * A * alpha * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 3) * A * beta**3 * np.arcsinh(A / a - beta / a)
            - 1 / 3 * A * beta**3 * np.arcsinh(B / a - beta / a)
            + (7 / 144) * A * beta**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 9 * A * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 4) * B**4 * np.arcsinh(B / a - alpha / a)
            - 1 / 4 * B**4 * np.arcsinh(B / a - beta / a)
            - 2 / 3 * B**3 * alpha * np.arcsinh(B / a - alpha / a)
            + (2 / 3) * B**3 * alpha * np.arcsinh(B / a - beta / a)
            - 7 / 16 * B**3 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (7 / 16) * B**3 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 4 * B**2 * a**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 4) * B**2 * a**2 * np.arcsinh(B / a - beta / a)
            + (1 / 2) * B**2 * alpha**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * B**2 * alpha**2 * np.arcsinh(B / a - beta / a)
            + (101 / 144)
            * B**2
            * alpha
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 8 / 9 * B**2 * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (3 / 16) * B**2 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 32) * B * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 32 * B * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 13 / 144 * B * alpha**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (1 / 4) * B * alpha**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 2 / 9 * B * alpha * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 16) * B * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 32) * a**4 * np.arcsinh(A / a - alpha / a)
            - 1 / 32 * a**4 * np.arcsinh(A / a - beta / a)
            - 1 / 32 * a**4 * np.arcsinh(B / a - alpha / a)
            + (1 / 32) * a**4 * np.arcsinh(B / a - beta / a)
            - 1 / 4 * a**2 * alpha**2 * np.arcsinh(A / a - alpha / a)
            + (1 / 4) * a**2 * alpha**2 * np.arcsinh(A / a - beta / a)
            + (1 / 4) * a**2 * alpha**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 4 * a**2 * alpha**2 * np.arcsinh(B / a - beta / a)
            - 55 / 288 * a**2 * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (2 / 9) * a**2 * alpha * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (55 / 288)
            * a**2
            * alpha
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 2 / 9 * a**2 * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 32 * a**2 * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 32) * a**2 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 12) * alpha**4 * np.arcsinh(A / a - alpha / a)
            - 1 / 12 * alpha**4 * np.arcsinh(B / a - alpha / a)
            + (25 / 144) * alpha**3 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 25 / 144 * alpha**3 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 2 * alpha**2 * beta**2 * np.arcsinh(A / a - beta / a)
            + (1 / 2) * alpha**2 * beta**2 * np.arcsinh(B / a - beta / a)
            - 3 / 4 * alpha**2 * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (3 / 4) * alpha**2 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (2 / 3) * alpha * beta**3 * np.arcsinh(A / a - beta / a)
            - 2 / 3 * alpha * beta**3 * np.arcsinh(B / a - beta / a)
            + (8 / 9) * alpha * beta**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 8 / 9 * alpha * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 4 * beta**4 * np.arcsinh(A / a - beta / a)
            + (1 / 4) * beta**4 * np.arcsinh(B / a - beta / a)
            - 5 / 16 * beta**3 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (5 / 16) * beta**3 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
        )
    elif (p, q) == (2, 2):
        return (
            -1 / 30 * A**5 * np.arcsinh(A / a - alpha / a)
            + (1 / 30) * A**5 * np.arcsinh(A / a - beta / a)
            + (1 / 6) * A**4 * alpha * np.arcsinh(A / a - alpha / a)
            - 1 / 6 * A**4 * alpha * np.arcsinh(A / a - beta / a)
            + (137 / 1800) * A**4 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 137 / 1800 * A**4 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 6) * A**3 * a**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 6 * A**3 * a**2 * np.arcsinh(A / a - beta / a)
            - 1 / 3 * A**3 * alpha**2 * np.arcsinh(A / a - alpha / a)
            + (1 / 3) * A**3 * alpha**2 * np.arcsinh(A / a - beta / a)
            - 137 / 450 * A**3 * alpha * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (25 / 72) * A**3 * alpha * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 77 / 1800 * A**3 * beta * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 3) * A**2 * B**3 * np.arcsinh(B / a - alpha / a)
            - 1 / 3 * A**2 * B**3 * np.arcsinh(B / a - beta / a)
            - A**2 * B**2 * alpha * np.arcsinh(B / a - alpha / a)
            + A**2 * B**2 * alpha * np.arcsinh(B / a - beta / a)
            - 11 / 18 * A**2 * B**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (11 / 18) * A**2 * B**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * A**2 * B * a**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * A**2 * B * a**2 * np.arcsinh(B / a - beta / a)
            + A**2 * B * alpha**2 * np.arcsinh(B / a - alpha / a)
            - A**2 * B * alpha**2 * np.arcsinh(B / a - beta / a)
            + (11 / 9)
            * A**2
            * B
            * alpha
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 3 / 2 * A**2 * B * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (5 / 18) * A**2 * B * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * A**2 * a**2 * alpha * np.arcsinh(A / a - alpha / a)
            + (1 / 2) * A**2 * a**2 * alpha * np.arcsinh(A / a - beta / a)
            + (1 / 2) * A**2 * a**2 * alpha * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * A**2 * a**2 * alpha * np.arcsinh(B / a - beta / a)
            - 607 / 3600 * A**2 * a**2 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (607 / 3600) * A**2 * a**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (2 / 9) * A**2 * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 2 / 9 * A**2 * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 3) * A**2 * alpha**3 * np.arcsinh(A / a - alpha / a)
            - 1 / 3 * A**2 * alpha**3 * np.arcsinh(B / a - alpha / a)
            - A**2 * alpha**2 * beta * np.arcsinh(A / a - beta / a)
            + A**2 * alpha**2 * beta * np.arcsinh(B / a - beta / a)
            + (137 / 300)
            * A**2
            * alpha**2
            * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 11 / 18 * A**2 * alpha**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 11
            / 18
            * A**2
            * alpha**2
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + A**2 * alpha**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + A**2 * alpha * beta**2 * np.arcsinh(A / a - beta / a)
            - A**2 * alpha * beta**2 * np.arcsinh(B / a - beta / a)
            + (13 / 72)
            * A**2
            * alpha
            * beta
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1
            / 2
            * A**2
            * alpha
            * beta
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 3 * A**2 * beta**3 * np.arcsinh(A / a - beta / a)
            + (1 / 3) * A**2 * beta**3 * np.arcsinh(B / a - beta / a)
            - 47 / 1800 * A**2 * beta**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 9) * A**2 * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * A * B**4 * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * A * B**4 * np.arcsinh(B / a - beta / a)
            + (4 / 3) * A * B**3 * alpha * np.arcsinh(B / a - alpha / a)
            - 4 / 3 * A * B**3 * alpha * np.arcsinh(B / a - beta / a)
            + (7 / 8) * A * B**3 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 7 / 8 * A * B**3 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * A * B**2 * a**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 2 * A * B**2 * a**2 * np.arcsinh(B / a - beta / a)
            - A * B**2 * alpha**2 * np.arcsinh(B / a - alpha / a)
            + A * B**2 * alpha**2 * np.arcsinh(B / a - beta / a)
            - 101
            / 72
            * A
            * B**2
            * alpha
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (16 / 9)
            * A
            * B**2
            * alpha
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 3 / 8 * A * B**2 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 16 * A * B * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (1 / 16) * A * B * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (13 / 72)
            * A
            * B
            * alpha**2
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 2 * A * B * alpha**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (4 / 9)
            * A
            * B
            * alpha
            * beta
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 8 * A * B * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 16 * A * a**4 * np.arcsinh(A / a - alpha / a)
            + (1 / 16) * A * a**4 * np.arcsinh(A / a - beta / a)
            + (1 / 16) * A * a**4 * np.arcsinh(B / a - alpha / a)
            - 1 / 16 * A * a**4 * np.arcsinh(B / a - beta / a)
            + (1 / 2) * A * a**2 * alpha**2 * np.arcsinh(A / a - alpha / a)
            - 1 / 2 * A * a**2 * alpha**2 * np.arcsinh(A / a - beta / a)
            - 1 / 2 * A * a**2 * alpha**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * A * a**2 * alpha**2 * np.arcsinh(B / a - beta / a)
            + (607 / 1800)
            * A
            * a**2
            * alpha
            * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 55
            / 144
            * A
            * a**2
            * alpha
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 55
            / 144
            * A
            * a**2
            * alpha
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (4 / 9) * A * a**2 * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (161 / 3600)
            * A
            * a**2
            * beta
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 1 / 16 * A * a**2 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 6 * A * alpha**4 * np.arcsinh(A / a - alpha / a)
            + (1 / 6) * A * alpha**4 * np.arcsinh(B / a - alpha / a)
            - 137 / 450 * A * alpha**3 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (25 / 72) * A * alpha**3 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + A * alpha**2 * beta**2 * np.arcsinh(A / a - beta / a)
            - A * alpha**2 * beta**2 * np.arcsinh(B / a - beta / a)
            + (11 / 9)
            * A
            * alpha**2
            * beta
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 3
            / 2
            * A
            * alpha**2
            * beta
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 4 / 3 * A * alpha * beta**3 * np.arcsinh(A / a - beta / a)
            + (4 / 3) * A * alpha * beta**3 * np.arcsinh(B / a - beta / a)
            - 101
            / 72
            * A
            * alpha
            * beta**2
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (16 / 9)
            * A
            * alpha
            * beta**2
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * A * beta**4 * np.arcsinh(A / a - beta / a)
            - 1 / 2 * A * beta**4 * np.arcsinh(B / a - beta / a)
            + (97 / 200) * A * beta**3 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 5 / 8 * A * beta**3 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 5) * B**5 * np.arcsinh(B / a - alpha / a)
            - 1 / 5 * B**5 * np.arcsinh(B / a - beta / a)
            - 1 / 2 * B**4 * alpha * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * B**4 * alpha * np.arcsinh(B / a - beta / a)
            - 17 / 50 * B**4 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (17 / 50) * B**4 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 6 * B**3 * a**2 * np.arcsinh(B / a - alpha / a)
            + (1 / 6) * B**3 * a**2 * np.arcsinh(B / a - beta / a)
            + (1 / 3) * B**3 * alpha**2 * np.arcsinh(B / a - alpha / a)
            - 1 / 3 * B**3 * alpha**2 * np.arcsinh(B / a - beta / a)
            + (97 / 200)
            * B**3
            * alpha
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 5 / 8 * B**3 * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (7 / 50) * B**3 * beta * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (2 / 225) * B**2 * a**2 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 2 / 225 * B**2 * a**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 47
            / 1800
            * B**2
            * alpha**2
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (1 / 9) * B**2 * alpha**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1
            / 8
            * B**2
            * alpha
            * beta
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 25) * B**2 * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (161 / 3600)
            * B
            * a**2
            * alpha
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 16 * B * a**2 * alpha * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (4 / 225)
            * B
            * a**2
            * beta
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 77 / 1800 * B * alpha**3 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (5 / 18)
            * B
            * alpha**2
            * beta
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 3
            / 8
            * B
            * alpha
            * beta**2
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (7 / 50) * B * beta**3 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 16) * a**4 * alpha * np.arcsinh(A / a - alpha / a)
            - 1 / 16 * a**4 * alpha * np.arcsinh(A / a - beta / a)
            - 1 / 16 * a**4 * alpha * np.arcsinh(B / a - alpha / a)
            + (1 / 16) * a**4 * alpha * np.arcsinh(B / a - beta / a)
            + (4 / 225) * a**4 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 4 / 225 * a**4 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 4 / 225 * a**4 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            + (4 / 225) * a**4 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 6 * a**2 * alpha**3 * np.arcsinh(A / a - alpha / a)
            + (1 / 6) * a**2 * alpha**3 * np.arcsinh(B / a - alpha / a)
            + (1 / 2) * a**2 * alpha**2 * beta * np.arcsinh(A / a - beta / a)
            - 1 / 2 * a**2 * alpha**2 * beta * np.arcsinh(B / a - beta / a)
            - 607
            / 3600
            * a**2
            * alpha**2
            * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            + (2 / 9) * a**2 * alpha**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (607 / 3600)
            * a**2
            * alpha**2
            * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 2 / 9 * a**2 * alpha**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 2 * a**2 * alpha * beta**2 * np.arcsinh(A / a - beta / a)
            + (1 / 2) * a**2 * alpha * beta**2 * np.arcsinh(B / a - beta / a)
            - 1
            / 16
            * a**2
            * alpha
            * beta
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (1 / 16)
            * a**2
            * alpha
            * beta
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 6) * a**2 * beta**3 * np.arcsinh(A / a - beta / a)
            - 1 / 6 * a**2 * beta**3 * np.arcsinh(B / a - beta / a)
            + (2 / 225) * a**2 * beta**2 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 2 / 225 * a**2 * beta**2 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 30) * alpha**5 * np.arcsinh(A / a - alpha / a)
            - 1 / 30 * alpha**5 * np.arcsinh(B / a - alpha / a)
            + (137 / 1800) * alpha**4 * np.sqrt(A**2 - 2 * A * alpha + a**2 + alpha**2)
            - 137 / 1800 * alpha**4 * np.sqrt(B**2 - 2 * B * alpha + a**2 + alpha**2)
            - 1 / 3 * alpha**2 * beta**3 * np.arcsinh(A / a - beta / a)
            + (1 / 3) * alpha**2 * beta**3 * np.arcsinh(B / a - beta / a)
            - 11
            / 18
            * alpha**2
            * beta**2
            * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (11 / 18)
            * alpha**2
            * beta**2
            * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            + (1 / 2) * alpha * beta**4 * np.arcsinh(A / a - beta / a)
            - 1 / 2 * alpha * beta**4 * np.arcsinh(B / a - beta / a)
            + (7 / 8) * alpha * beta**3 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            - 7 / 8 * alpha * beta**3 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
            - 1 / 5 * beta**5 * np.arcsinh(A / a - beta / a)
            + (1 / 5) * beta**5 * np.arcsinh(B / a - beta / a)
            - 17 / 50 * beta**4 * np.sqrt(A**2 - 2 * A * beta + a**2 + beta**2)
            + (17 / 50) * beta**4 * np.sqrt(B**2 - 2 * B * beta + a**2 + beta**2)
        )
    else:
        raise ValueError(f"(p, q) = ({p}, {q}) not in [0, 2]²")
